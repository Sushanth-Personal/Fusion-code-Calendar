/* Monthly View Grid */
.mGrid {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    grid-template-rows: repeat(6, 1fr);
    gap: 2px;
    background-color: white;
    padding: 10px;
}

/* Regular Day Cell */
.mDay {
    border: 1px solid #ddd;
    padding: 10px;
    min-height: 100px;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    font-size: 1rem;
    font-weight: bold;
    position: relative;
}

/* 🎨 Current Month Days (February) - Blue Gradient */
.mDay.currentMonth {
    background: linear-gradient(135deg, #a2d2ff, #3a86ff);
    color: white;
    border: 2px solid #3a86ff;
}